title: Textcontainer
fields:
  -
    handle: color
    field:
      options:
        black: Schwarz
        teal: Grün
      default: black
      type: select
      display: Textfarbe
  -
    handle: text
    field:
      buttons:
        - removeformat
        - anchor
      remove_empty_nodes: true
      type: bard
      display: Text
      toolbar_mode: floating
      container: assets
  -
    handle: margin_bottom
    field:
      options:
        narrow: 'Schmal (40px)'
        default: 'Normal (60px)'
      default: default
      type: select
      display: 'Abstand (nach unten)'
